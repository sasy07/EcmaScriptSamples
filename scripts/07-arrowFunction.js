//Arrow Function
const getSumArrowFunction = (a, b) => {
    return a + b;
  };
  console.log(getSumArrowFunction(2, 5));
  
  //Minimal Arrow Function => We can remove () : (a) => a
  const getSumWithTwo = (a) => a + 2;
  console.log(getSumWithTwo(4));
  